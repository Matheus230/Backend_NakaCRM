# =============================================================================
# EXEMPLO DE VARIÁVEIS DE AMBIENTE - Backend NakaCRM
# =============================================================================
# Copie este arquivo para .env e preencha com valores reais
# NUNCA commite o arquivo .env ao git!

# =============================================================================
# DATABASE
# =============================================================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crm_db
DB_USER=crm_user
DB_PASSWORD=crm_password

# =============================================================================
# JWT SECURITY
# =============================================================================
# Gere uma chave secreta forte com: openssl rand -hex 64
JWT_SECRET=your-secure-jwt-secret-key-here-minimum-256-bits
JWT_EXPIRATION=86400000
JWT_REFRESH_EXPIRATION=604800000

# =============================================================================
# OAUTH2 GOOGLE
# =============================================================================
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# =============================================================================
# EMAIL CONFIGURATION
# =============================================================================
# Escolha seu provedor e configure as variáveis correspondentes:

# --- GMAIL (Recomendado) ---
# 1. Ative verificação em 2 etapas: https://myaccount.google.com/security
# 2. Gere uma senha de app: https://myaccount.google.com/apppasswords
# 3. Use a senha de app gerada no MAIL_PASSWORD
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=seu-email@gmail.com
MAIL_PASSWORD=sua-senha-de-app-aqui
MAIL_SMTP_AUTH=true
MAIL_STARTTLS=true
MAIL_DEBUG=false

# --- OUTLOOK/HOTMAIL ---
# MAIL_HOST=smtp-mail.outlook.com
# MAIL_PORT=587
# MAIL_USERNAME=seu-email@outlook.com
# MAIL_PASSWORD=sua-senha
# MAIL_SMTP_AUTH=true
# MAIL_STARTTLS=true

# --- YAHOO MAIL ---
# MAIL_HOST=smtp.mail.yahoo.com
# MAIL_PORT=587
# MAIL_USERNAME=seu-email@yahoo.com
# MAIL_PASSWORD=sua-senha-de-app
# MAIL_SMTP_AUTH=true
# MAIL_STARTTLS=true

# --- SENDGRID (API) ---
# MAIL_HOST=smtp.sendgrid.net
# MAIL_PORT=587
# MAIL_USERNAME=apikey
# MAIL_PASSWORD=SG.sua-api-key-aqui
# MAIL_SMTP_AUTH=true
# MAIL_STARTTLS=true

# --- MAILHOG (Desenvolvimento Local) ---
# docker run -d -p 1025:1025 -p 8025:8025 mailhog/mailhog
# Acesse http://localhost:8025 para ver os emails
# MAIL_HOST=localhost
# MAIL_PORT=1025
# MAIL_USERNAME=
# MAIL_PASSWORD=
# MAIL_SMTP_AUTH=false
# MAIL_STARTTLS=false

# =============================================================================
# APPLICATION
# =============================================================================
APP_BASE_URL=http://localhost:8080
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# =============================================================================
# SECURITY
# =============================================================================
ADMIN_EMAIL=admin@empresa.com
ADMIN_PASSWORD=ChangeMe123!
